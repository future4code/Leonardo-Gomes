```
function contaOcorrencias(arrayDeNumeros, numeroEscolhido) {
  const vezesRepetidas = 0
  const mensagem;
  for(vezesRepetidas of arrayDeNumeros)
  if(numero === numeroEscolhido){
    vezesRepetidas+=1 
    mensagem = `O número ${numeroEscolhido} aparece ${vezesRepetidas}x`
  } else if (vezesRepetidas === 0){
    const mensagem = "Número não encontrado"
    return mensagem
  }

```